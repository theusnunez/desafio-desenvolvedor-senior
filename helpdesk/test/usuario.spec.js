/*
  Test skeleton: add integration tests using Adonis' test runner (japa)
  Example tests should be implemented to cover:
   - Creating user (password hashed)
   - Updating user (password hashed when provided)
   - Creating chamado with attachment
*/

'use strict'

// TODO: implement tests

const { test } = use('Test/Suite')('Usuario')

test('placeholder', async ({ assert }) => {
  assert.isTrue(true)
})
